name: Generate CHANGELOG

on:
  # sprint → dev PR merge 시 CHANGELOG 생성
  # dev가 진실의 원천 (source of truth)
  pull_request:
    branches: [dev]
    types: [opened, synchronize, closed]

jobs:
  changelog:
    # PR 체크에는 나타나지만, 실제 실행은 merge 후에만
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Check if PR is merged
        id: check-merged
        run: |
          # CHANGELOG 동기화 PR은 스킵 (무한 루프 방지)
          PR_TITLE="${{ github.event.pull_request.title }}"
          PR_AUTHOR="${{ github.event.pull_request.user.login }}"
          
          if echo "$PR_TITLE" | grep -qiE "(sync.*changelog|docs:.*sync|changelog.*sync)"; then
            echo "⏭️ Skipping: This is a CHANGELOG sync PR from main → dev"
            echo "PR title: $PR_TITLE"
            echo "merged=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          if [ "$PR_AUTHOR" == "github-actions[bot]" ]; then
            echo "⏭️ Skipping: PR created by github-actions[bot] (likely sync PR)"
            echo "merged=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          if [ "${{ github.event.pull_request.merged }}" != "true" ]; then
            echo "⏭️ PR not merged yet. This workflow will run automatically after PR merge."
            echo "merged=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "✅ PR merged. Proceeding with CHANGELOG generation."
          echo "merged=true" >> $GITHUB_OUTPUT

      - name: Checkout repository
        if: steps.check-merged.outputs.merged == 'true'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install git-cliff
        if: steps.check-merged.outputs.merged == 'true'
        run: |
          wget -q https://github.com/orhun/git-cliff/releases/download/v2.10.1/git-cliff-2.10.1-x86_64-unknown-linux-musl.tar.gz
          tar -xzf git-cliff-2.10.1-x86_64-unknown-linux-musl.tar.gz
          chmod +x git-cliff-2.10.1/git-cliff
          sudo mv git-cliff-2.10.1/git-cliff /usr/local/bin/

      - name: Generate CHANGELOG
        if: steps.check-merged.outputs.merged == 'true'
        id: git-cliff
        run: |
          echo "🔍 Generating CHANGELOG..."
          git cliff --config cliff.toml --strip header > CHANGELOG_NEW.md
          if ! cmp -s CHANGELOG.md CHANGELOG_NEW.md 2>/dev/null; then
            echo "✅ CHANGELOG has changes"
            mv CHANGELOG_NEW.md CHANGELOG.md
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "ℹ️ No changes in CHANGELOG"
            rm -f CHANGELOG_NEW.md
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit CHANGELOG changes
        if: |
          steps.check-merged.outputs.merged == 'true' &&
          steps.git-cliff.outputs.has_changes == 'true'
        run: |
          echo "💾 Committing CHANGELOG changes..."
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add CHANGELOG.md
          git commit -m "docs: update CHANGELOG

          Automatic CHANGELOG update from commit messages.

          - Technical Implementation: Generated using git-cliff
          - Design Decisions: Based on conventional commit format
          - Alternatives: Manual updates considered but chose automation

          Impact: Ensures consistent and accurate project documentation."
          echo "✅ CHANGELOG commit created"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Push changes
        if: |
          steps.check-merged.outputs.merged == 'true' &&
          steps.git-cliff.outputs.has_changes == 'true'
        run: |
          # dev 브랜치로 push (진실의 원천)
          TARGET_BRANCH="${{ github.event.pull_request.base.ref }}"
          echo "🚀 Pushing CHANGELOG to dev branch: $TARGET_BRANCH..."
          git push origin HEAD:$TARGET_BRANCH
          echo "✅ Changes pushed successfully to dev"
          echo "ℹ️  dev is the source of truth for CHANGELOG"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Log skip reason
        if: |
          steps.check-merged.outputs.merged == 'true' &&
          steps.git-cliff.outputs.has_changes != 'true'
        run: |
          echo "⏭️ Skipping CHANGELOG commit - no changes detected"
          echo "Change detection result: '${{ steps.git-cliff.outputs.has_changes }}'"
          echo "This usually means:"
          echo "  - No new conventional commits since last tag/release"
          echo "  - Generated CHANGELOG content is identical to existing file"
          echo "  - Commits don't match any configured commit_parsers patterns"

      - name: Log success
        if: |
          steps.check-merged.outputs.merged == 'true' &&
          steps.git-cliff.outputs.has_changes == 'true'
        run: |
          echo "✅ CHANGELOG successfully updated and pushed to dev!"
          echo "Event: ${{ github.event_name }}"
          echo "PR: ${{ github.event.pull_request.title }}"
          echo "Base branch: ${{ github.event.pull_request.base.ref }}"
          echo ""
          echo "📝 Workflow Strategy:"
          echo "  - dev is the source of truth for CHANGELOG"
          echo "  - sprint → dev: CHANGELOG auto-generated"
          echo "  - dev → main: CHANGELOG naturally flows (no sync needed)"
