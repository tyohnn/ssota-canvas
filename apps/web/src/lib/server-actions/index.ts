export { withSecureAction } from './with-secure-action';
export type {
  SecureAction,
  SecureActionOptions,
  PageIdExtractor,
  PageIdResult,
  MetadataExtractor,
  RateLimitConfig,
  CacheConfig,
  ActionContext,
} from './types';
