/**
 * App System shared types.
 * Block view mode (original / note / card) is a global block-management concept;
 * it is not defined here.
 */

export type AppCategory = 'built-in' | 'first-party' | 'community';
