/**
 * Block Management - 공통 타입들
 *
 * Position, Size는 canvas-management에서 import
 */

// 공통 타입들을 canvas-management에서 import
export type {
  Position,
  Size,
} from '../../../canvas-management/shared/types/common.types';
